[["%",{"text":"C_Sequence_Transformation.cpp","registerMode":0}],["*",{"text":"#include <bits/stdc++.h>\nusing namespace std;\n\nvoid solve() {\n    int n;\n    cin >> n;\n    vector<int> arr(n);\n    for (auto& i: arr) cin >> i;\n    int ans = 0;\n    map<int, int> range, cnt;\n    for (auto& i: arr) ++cnt[i];\n    for (int i = 0; i < n-1; ++i) {\n        if (arr[i] != arr[i+1]) ++range[arr[i]];\n    }\n    if (cnt[arr[n-1]] == 1) range[arr[n-1]] = 1;\n    if (range.empty()) cout << \"0\\n\";\n    else cout << (*range.begin()).second << \"\\n\";\n}\n\nint main() {\n    ios_base::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int t = 1;\n    cin >> t;\n    while (t--) {\n        solve();\n    }\n}\n","registerMode":2}],["0",{"text":"#include <bits/stdc++.h>\nusing namespace std;\n\nvoid solve() {\n    int n;\n    cin >> n;\n    vector<int> arr(n);\n    for (auto& i: arr) cin >> i;\n    int ans = 0;\n    map<int, int> range, cnt;\n    for (auto& i: arr) ++cnt[i];\n    for (int i = 0; i < n-1; ++i) {\n        if (arr[i] != arr[i+1]) ++range[arr[i]];\n    }\n    if (cnt[arr[n-1]] == 1) range[arr[n-1]] = 1;\n    if (range.empty()) cout << \"0\\n\";\n    else cout << (*range.begin()).second << \"\\n\";\n}\n\nint main() {\n    ios_base::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int t = 1;\n    cin >> t;\n    while (t--) {\n        solve();\n    }\n}\n","registerMode":2}]]